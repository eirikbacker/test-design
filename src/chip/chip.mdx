
import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import * as stories from './chip.stories';

<Meta of={stories} />

# Chip

## Kode
- Bruk klassen `chip` på typisk `<button>`
<Canvas of={stories.Default} />
