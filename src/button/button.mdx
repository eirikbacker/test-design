import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import * as stories from './button.stories';

<Meta of={stories} />

# Button

## Kode
- Bruk klassen `button` på typisk `<button>`
<Canvas of={stories.Default} />
